import React from 'react';
import { Header, Intro } from './Components';

const app = () => {
    return (
        <>
            <Header />
            <Intro />

        </>
    );
};

export default app;