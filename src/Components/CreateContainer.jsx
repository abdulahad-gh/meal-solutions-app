import React from 'react';

const CreateContainer = () => {
    return (
        <section className='flex flex-col justify-center items-center h-screen '>
            This is create container component.

        </section>
    );
};

export default CreateContainer;